# Session sign-off — 2026-07-20

Operator capture for Premium Book work closed this day. Durable how-to stays in [README.md](README.md).

## What shipped today

- **Auto structure** — sell premium picks `bull_put` / `bear_call` from underlying ST1+ST2; flat/whipsaw → sit out
- **Short straddle / strangle retired** for new sell entries (legacy legs still manage / convert / exit)
- **Candle-close gates** — `exit_on_bar_close_only` + desk-wide same-bar stamps; ATR / Force / fixed SL stay tick-live
- **CE churn / ST1 fix** — naked short CE/PE ST1 exit uses **adverse** zone (not the opposite flags)
- **Disarm + orphan CE** — `*_exit_blocked_disarm` clarified; ARM → Force close / Close all; broker-flat reconcile without ARM when confirmed flat
- **`entry_skipped_same_bar`** (and related) documented — wait for next bar, not “signal broken”
- **ADX defaults 14 / 20** aligned with desk `DEFAULT_ADX`
- **Signal vs “Bull Ready”** — live Signal / Active structure vs Structure dropdown preference
- **Futures vs options** — entry/ST1 on underlying (index/futures); ATR/SL on option LTP
- **Dual-open poll bug** — status/config poll overwrote checkbox; noted as legacy-relevant
- **Status reconcile** — `*_exit_reconciled_broker_flat` when local open but Kite flat

## Operator reminders

1. **Restart the API** after pulling these changes so the runner loads the new logic.
2. **Orphan CE:** Live → **ARM** → **Force close** (Premium Book) or **Close all** (Rolling Straddle if RS-owned). Confirm on Kite.
3. **Flat / whipsaw → sit out** — do not expect a short strangle.
4. **New sell entries = credit verticals only** (`bull_put` / `bear_call`). ATM straddles → Rolling Straddle.

## Explicitly excluded from this save

**Mumbai VPS Latency Plan** — discussed separately; **not** part of this documentation save and not claimed as shipped here.

---

See [README.md](README.md) for the full operator guide.
